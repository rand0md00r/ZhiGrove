基本思路：
1. 把 Flow Matching 动作生成器当成 TRM 里的 answer-updater f_H，在它外面包一层 TRM 风格的「多步潜在推理 z + 多轮动作计划 y 的递归 refinement」+ 深监督训练。

# 问题抽象

1. TRM 的本质是：

* **输入**：

  * 问题 / 条件：(x)（ARC 拼图、迷宫、数独等）
  * 当前解：(y)（当前预测的输出网格）
  * 潜在推理状态：(z)（「怎么推理到当前解」的隐变量）

* **递归结构**（不考虑 ACT 的简化版本）：
